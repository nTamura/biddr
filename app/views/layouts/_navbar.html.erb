<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">

    <ul class="nav navbar-nav">
      <li class="<%= 'active' if current_page?(root_path) %>"><%= link_to 'Biddr', root_path %></li>
    </ul>

    <ul class="nav navbar-nav navbar-right">
      <li class="<%= 'active' if current_page?(auctions_path) %>"><%= link_to 'Auctions', auctions_path %></li>

      <% if user_signed_in? %>
        <li class="<%= 'active' if current_page?(user_favorites_path(current_user)) %>"><%= link_to 'Favorites', user_favorites_path(current_user) %></li>
        <li class="<%= 'active' if current_page?(user_path(current_user)) %>"><%= link_to 'Profile', user_path(current_user) %></li>

        <li><%= link_to "#{current_user.username}: Log Out", sessions_path, method: :delete, data: {confirm: 'Logout?'} %></li>

      <% else %>

        <li><%= link_to 'Log In', new_session_path %></li>
        <li><%= link_to 'Sign Up', new_user_path %></li>

      <% end %>

    </ul>
  </div>
</nav>
